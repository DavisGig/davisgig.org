# davisgig.org website

## installation

* `npm install`
* `bower install`
* `gulp`

## running

* `cd build/dev`
* `python -m http.server`
* view app at [http://localhost:8000](http://localhost:8000)
